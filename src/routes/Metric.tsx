import React from "react";

const Metric = () => {
  return (
    <div className="flex justify-between items-center w-inherit">
      This is the metric screen
    </div>
  );
};

export default Metric;
